#include <stdio.h>
#include <math.h>

int main() {
    double R, volume;

    scanf("%lf", &R);

    printf("VOLUME = %.3lf", volume = ((3.14159) * pow (R, 3) * (4.0 / 3.0)));

    return 0;
}